<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/meuscursos.css" />
    <link rel="stylesheet" href="/css/header2.css">
    <link rel="shortcut icon" href="../../Images/favicon.ico" type="image/x-icon">
    <title>Meus Cursos</title>
  </head>
  <body>
    <header>
      <button onclick="history.back()" class="back-button">
        <img src="../images/voltar.svg" alt="" />
      </button>
      <p>MEUS CURSOS</p>
      <section>
        <p><%= user.NOME_USU || 'Aluno' %></p>
        <button class="user-icon" id="userIcon" aria-label="Abrir menu do usuário">
            <img src="/aluno/foto-perfil/<%= user.ID_USUARIO %>?v=<%= timestamp %>" alt="imagem do perfil" style="width: 48px; height: 48px; object-fit: cover; border-radius: 50%; border: 2px solid #ccc;"
            onerror="this.onerror=null; this.src='/images/perfil.svg';"/>
            <section class="user-config" id="userConfig">
                <h2><%= user.NOME_USU || 'Aluno' %></h2>
                <ul>
                    <li><a href="/aluno/a-perfil">MENU</a></li>
                    <li><a href="/aluno/a-config">CONFIGURAÇÕES</a></li>
                    <li><a href="/aluno/logout">SAIR</a></li>
                </ul>
            </section>
        </button>
    </section>
    </header>
    <main>
      <section class="meuscursos">
        <section class="topic">
          <h2>NOME</h2>
          <h2>CATEGORIA</h2>
          <h2>%</h2>
        </section>
        
        <% if (cursos && cursos.length > 0) { %>
          <% cursos.forEach(function(curso) { %>
            <section class="cursos">
              <p><%= curso.TITULO %></p>
              <section class="download-section">
                <span><%= curso.CATEGORIA || 'Geral' %></span>
                <span class="porcentagem"><%= curso.PORCENTAGEM %>%</span>
              </section>
            </section>
          <% }); %>
        <% } else { %>
          <section class="cursos">
            <p>Você ainda não possui nenhum curso.</p>
            <section class="download-section">
              <span>-</span>
              <span class="porcentagem">0%</span>
            </section>
          </section>
        <% } %>

        <section class="vermais">
          <a href="/aluno/a-todososcursos">TODOS OS MEUS CURSOS</a>
          <a href="/aluno/a-certificado">MEUS CERTIFICADOS</a>
        </section>
      </section>
      <script src="/js/icon-menu.js"></script>
    </main>
  </body>
</html>